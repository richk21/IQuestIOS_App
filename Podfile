# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'IQuest-VIT' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IQuest-VIT
  pod 'Firebase/Storage'
  pod 'Firebase/Database'
  pod 'Firebase', '>=2.5.0'
  pod 'CardSlider'

end
